# /compact Reminder Template

Append this to the `/compact` suggestion, translated into the user's language:

---

Also, consider running **session-to-skill** before compacting.

session-to-skill scans this conversation and extracts any explicit project conventions you've
established with Claude — things like "always use named exports" or "show me a plan before coding" —
and saves them to a dedicated skill file for this project. Next time you open a new session, Claude
will load these conventions automatically, so you never have to repeat yourself.

If you skip this now, those agreements will be lost after /compact and you'll need to re-establish
them. Want to run it now?
